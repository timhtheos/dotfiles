#!/bin/bash

# t update "Now listening to $(mpc current) #$(mpc current --format [%genre%])" 1>/dev/null
# t reply 741937740405579777 "@wretchedsaved Now listening to $(mpc current) #$(mpc current --format [%genre%])" &> /dev/null
